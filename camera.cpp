#include "camera.h"

Camera::Camera(Vec3 position, float distanceToScreen, float screenWidth, float pixelWidth)
: position(position), 
distanceToScreen(distanceToScreen), 
screenWidth(screenWidth), 
pixelWidth(pixelWidth)
{
	

}